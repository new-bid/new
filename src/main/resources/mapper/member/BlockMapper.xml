<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.bideo.mapper.member.BlockMapper">

    <select id="existsBlock" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tbl_block
            WHERE blocker_id = #{blockerId}
              AND blocked_id = #{blockedId}
        )
    </select>

    <insert id="insertBlock">
        INSERT INTO tbl_block (blocker_id, blocked_id)
        VALUES (#{blockerId}, #{blockedId})
    </insert>

    <delete id="deleteBlock">
        delete from tbl_block
        where blocker_id = #{blockerId}
          and blocked_id = #{blockedId}
    </delete>

    <select id="selectBlockedMembers" resultType="com.app.bideo.dto.member.BlockResponseDTO">
        select
            b.id,
            b.blocked_id,
            m.nickname,
            m.profile_image,
            b.created_datetime
        from tbl_block b
        join tbl_member m on b.blocked_id = m.id
        where b.blocker_id = #{blockerId}
        order by b.created_datetime desc
        limit #{limit}
        offset #{offset}
    </select>

</mapper>
