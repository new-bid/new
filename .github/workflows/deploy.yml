name: Deploy to EC2

on:
  push:
    branches:
      - master

jobs:
  deploy:
    if: github.repository == 'new-bid/new'
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        include:
          - name: EC2_HOST1
          - name: EC2_HOST2

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Build application jar
        run: chmod +x ./gradlew && ./gradlew --no-daemon --console=plain --max-workers=1 bootJar -x test

      - name: Prepare deployment jar
        run: |
          ls -al build/libs
          JAR_FILE="$(find build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*-plain.jar' | head -n 1)"
          test -n "$JAR_FILE"
          cp "$JAR_FILE" app.jar

      - name: Set HOST env var from secrets
        run: echo "HOST=${{ secrets[matrix.name] }}" >> $GITHUB_ENV

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H $HOST >> ~/.ssh/known_hosts

      - name: Copy project to EC2
        run: |
          rsync -avz --delete --delete-excluded \
            --exclude='.git' \
            --exclude='.idea' \
            --exclude='.gradle' \
            --exclude='out' \
            --exclude='.vscode' \
            --exclude='.claude' \
            -e "ssh -i ~/.ssh/id_rsa" . ${{ secrets.EC2_USER }}@$HOST:/home/ubuntu/bideo

      - name: Debug secret presence
        run: |
          echo "PSQL_PORT length: ${#X1}"
          echo "PSQL_DATABASE length: ${#X2}"
          echo "PSQL_USERNAME length: ${#X3}"
          echo "PSQL_PASSWORD length: ${#X4}"
          echo "REDIS_PORT length: ${#X5}"
          echo "EC2_HOST length: ${#X6}"
          echo "JWT_SECRET length: ${#X7}"
          echo "AWS_BUCKET_NAME length: ${#A1}"  
          echo "FASTAPI_BASE_URL length: ${#A2}"
          echo "BOOTPAY_PRIVATE_KEY length: ${#A3}"
          echo "BOOTPAY_REST_CLIENT_KEY length: ${#A4}"
          echo "SOLAPI_API_SECRET length: ${#A5}"
          echo "SPRING_MAIL_PASSWORD length: ${#A6}"
          echo "EC2_USER length: ${#A7}"
          echo "EC2_KEY length: ${#A8}"
        
        env:
          X1: ${{ secrets.PSQL_PORT }}
          X2: ${{ secrets.PSQL_DATABASE }}
          X3: ${{ secrets.PSQL_USERNAME }}
          X4: ${{ secrets.PSQL_PASSWORD }}
          X5: ${{ secrets.REDIS_PORT }}
          X6: ${{ secrets.EC2_HOST }}
          X7: ${{ secrets.JWT_SECRET }}
          A1: ${{ secrets.AWS_BUCKET_NAME }}
          A2: ${{ secrets.FASTAPI_BASE_URL || secrets.ML_API_BASE_URL }}
          A3: ${{ secrets.BOOTPAY_PRIVATE_KEY }}
          A4: ${{ secrets.BOOTPAY_REST_CLIENT_KEY || secrets.BOOTPAY_REST_API_KEY }}
          A5: ${{ secrets.SOLAPI_API_SECRET }}
          A6: ${{ secrets.SPRING_MAIL_PASSWORD }}
          A7: ${{ secrets.EC2_USER }}
          A8: ${{ secrets.EC2_KEY }}

      - name: Ensure auction database tables
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.EC2_USER }}@$HOST << EOF
            set -e
            cd /home/ubuntu/bideo
            docker run --rm \
              --add-host=bideo.shop:172.31.37.54 \
              -v /home/ubuntu/bideo/src/main/resources/sql:/sql \
              -e PGPASSWORD=${{ secrets.PSQL_PASSWORD }} \
              postgres:16 \
              psql -v ON_ERROR_STOP=1 \
                -h bideo.shop \
                -p ${{ secrets.PSQL_PORT }} \
                -U ${{ secrets.PSQL_USERNAME }} \
                -d ${{ secrets.PSQL_DATABASE }} \
                -f /sql/2026-05-20_create_missing_auction_tables.sql
          EOF

      - name: Build & Run Docker on EC2
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.EC2_USER }}@$HOST << EOF
            set -e
            cd /home/ubuntu/bideo
            test -f app.jar
            docker build -t bideo .
            docker ps -a
            docker rm -f bideo || true
            docker run -d --restart unless-stopped -p 80:10000 --name bideo \
              --add-host=bideo.shop:172.31.37.54 \
              -e EC2_HOST=${{ secrets.EC2_HOST }} \
              -e PSQL_PORT=${{ secrets.PSQL_PORT }} \
              -e PSQL_DATABASE=${{ secrets.PSQL_DATABASE }} \
              -e PSQL_USERNAME=${{ secrets.PSQL_USERNAME }} \
              -e PSQL_PASSWORD=${{ secrets.PSQL_PASSWORD }} \
              -e REDIS_PORT=${{ secrets.REDIS_PORT }} \
              -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
              -e AWS_ACCESS_KEY=${{ secrets.AWS_ACCESS_KEY }} \
              -e AWS_SECRET_KEY=${{ secrets.AWS_SECRET_KEY }} \
              -e AWS_BUCKET_NAME=${{ secrets.AWS_BUCKET_NAME }} \
              -e AWS_REGION=${{ secrets.AWS_REGION }} \
              -e KAKAO_CLIENT_ID=${{ secrets.KAKAO_CLIENT_ID }} \
              -e KAKAO_CLIENT_SECRET=${{ secrets.KAKAO_CLIENT_SECRET }} \
              -e NAVER_CLIENT_ID=${{ secrets.NAVER_CLIENT_ID }} \
              -e NAVER_CLIENT_SECRET=${{ secrets.NAVER_CLIENT_SECRET }} \
              -e GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }} \
              -e GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }} \
              -e BOOTPAY_JS_APPLICATION_ID=${{ secrets.BOOTPAY_JS_APPLICATION_ID || secrets.BOOTPAY_JS_API_KEY }} \
              -e BOOTPAY_REST_CLIENT_KEY=${{ secrets.BOOTPAY_REST_CLIENT_KEY || secrets.BOOTPAY_REST_API_KEY }} \
              -e BOOTPAY_PRIVATE_KEY=${{ secrets.BOOTPAY_PRIVATE_KEY }} \
              -e SPRING_MAIL_USERNAME=${{ secrets.SPRING_MAIL_USERNAME }} \
              -e SPRING_MAIL_PASSWORD=${{ secrets.SPRING_MAIL_PASSWORD }} \
              -e APP_MAIL_FROM_ADDRESS=${{ secrets.APP_MAIL_FROM_ADDRESS }} \
              -e SOLAPI_API_KEY=${{ secrets.SOLAPI_API_KEY }} \
              -e SOLAPI_API_SECRET=${{ secrets.SOLAPI_API_SECRET }} \
              -e SOLAPI_FROM_NUMBER=${{ secrets.SOLAPI_FROM_NUMBER }} \
              -e FASTAPI_BASE_URL=${{ secrets.FASTAPI_BASE_URL || secrets.ML_API_BASE_URL }} \
              bideo
            sleep 5
            docker ps -a --filter name=bideo
            docker logs --tail 200 bideo
          EOF
          
      


      - name: Clean up dangling images
        if: success()
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.EC2_USER }}@$HOST << 'EOF'
            docker image prune -f
            docker container prune -f
          EOF
